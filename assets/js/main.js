const header=document.getElementById('header'),menu=document.getElementById('nav-menu'),menuToggle=document.getElementById('nav-toggle'),themeToggle=document.getElementById('theme-toggle'),links=document.querySelectorAll('#nav-menu a'),sections=document.querySelectorAll('main section[id]');
function setMenu(open){menu.classList.toggle('open',open);document.body.classList.toggle('menu-open',open);menuToggle.setAttribute('aria-expanded',String(open));menuToggle.setAttribute('aria-label',open?'Close navigation':'Open navigation');menuToggle.innerHTML=`<i class="uil ${open?'uil-times':'uil-bars'}"></i>`}
menuToggle.addEventListener('click',()=>setMenu(!menu.classList.contains('open')));links.forEach(link=>link.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});
const saved=localStorage.getItem('portfolio-theme'),prefersDark=window.matchMedia('(prefers-color-scheme: dark)').matches;if(saved==='dark'||(!saved&&prefersDark))document.body.classList.add('dark');
function updateTheme(){const dark=document.body.classList.contains('dark');themeToggle.innerHTML=`<i class="uil ${dark?'uil-sun':'uil-moon'}"></i>`;themeToggle.setAttribute('aria-label',dark?'Switch to light theme':'Switch to dark theme')}
updateTheme();themeToggle.addEventListener('click',()=>{document.body.classList.toggle('dark');localStorage.setItem('portfolio-theme',document.body.classList.contains('dark')?'dark':'light');updateTheme()});
function updateNav(){header.classList.toggle('scrolled',scrollY>20);let current='';sections.forEach(section=>{if(scrollY>=section.offsetTop-160)current=section.id});links.forEach(link=>link.classList.toggle('active',link.getAttribute('href')===`#${current}`))}addEventListener('scroll',updateNav,{passive:true});updateNav();
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.getElementById('year').textContent=new Date().getFullYear();
